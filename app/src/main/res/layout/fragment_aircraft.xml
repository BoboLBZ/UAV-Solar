<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.hitices.autopatrol.AircraftFragment"
    android:orientation="vertical"
    android:gravity="center_horizontal">
    <TextView
        android:id="@+id/aircraft_type"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"
        android:text="@string/aircraft_unknown" />
    <View
        android:id="@+id/divLine"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_below="@id/aircraft_type"
        android:background="@color/colorPrimary"/>

    <TextView
        android:id="@+id/camera_type"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_alignStart="@+id/aircraft_type"
        android:layout_below="@+id/divLine"
        android:text="@string/camera_unknown" />
    <View
        android:id="@+id/divLine1"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_below="@id/camera_type"
        android:background="@color/colorPrimary"/>
    <Spinner
        android:id="@+id/missionSelected"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_below="@id/divLine1"
        android:layout_centerHorizontal="true"
        android:spinnerMode="dropdown"
        android:theme="@android:style/Theme.Holo.Light">
    </Spinner>

    <SeekBar
        android:id="@+id/seekBar_begin"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="18dp"
        android:layout_centerHorizontal="true"
        android:thumb="@drawable/ic_flight_takeoff_unpressed" />
    <ImageButton
        android:id="@+id/btn_take_off"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="18dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/ic_flight_takeoff_unpressed"/>
</RelativeLayout>
